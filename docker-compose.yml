version: '3.8'

services:

  frontend:
    build: ./frontend
    ports:
      - "8081:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - "5000:80"
    depends_on:
      - db

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      SA_PASSWORD: "ITPL#2020@Inv"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
