services:

  frontend:
    build: ./frontend
    container_name: angular_frontend
    ports:
      - "8081:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: dotnet_backend
    ports:
      - "5000:80"
